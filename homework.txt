#hw1
Make the button change the text of the heading.
#bonus
multiple headings to the page, make button change the second one
#super bonus
*Add a form to the page
*Add a text input to the form
*Update the heading with the text that you type in the text input

#hw2
*Create a new function called 'renderColor' that returns a 'div' 
element
*Call that function when adding that 'div' to 'colorItem'
#bonus
*Create a new function called 'renderListItem'
*Use it to create the list items for each stat.
#super bonus
*Create a new function called 'renderList'
*Use it to create the list items for each person's stat.
*Call 'renderListItem' from 'renderList', not directly from 
'handleSubmit'
#super mega hyper bonus
**Do not use innerHTML, keep using document.createElement